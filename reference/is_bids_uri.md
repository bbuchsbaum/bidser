# Test whether an object is a BIDS URI

Test whether an object is a BIDS URI

## Usage

``` r
is_bids_uri(x)
```

## Arguments

- x:

  Any object.

## Value

`TRUE` if `x` inherits from `"bids_uri"`, `FALSE` otherwise.
