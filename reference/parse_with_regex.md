# Parse a BIDS filename using regex

Parse a BIDS filename using regex

## Usage

``` r
parse_with_regex(filename, spec)
```

## Arguments

- filename:

  The filename to parse

- spec:

  The specification object

## Value

A list with parsed components or NULL if no match
