% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datatype_registry.R
\name{register_datatype}
\alias{register_datatype}
\title{Register a custom BIDS datatype}
\usage{
register_datatype(
  name,
  spec,
  parser_fn,
  folder = name,
  scope = c("raw", "derivative", "both"),
  overwrite = FALSE
)
}
\arguments{
\item{name}{A non-empty character string naming the datatype.}

\item{spec}{A spec list as returned by \code{func_spec()} etc.}

\item{parser_fn}{A parser object as returned by \code{func_parser()} etc.}

\item{folder}{The folder name to scan inside each subject/session directory.
Defaults to \code{name}.}

\item{scope}{One of \code{"raw"}, \code{"derivative"}, or \code{"both"}.}

\item{overwrite}{Logical.  If \code{FALSE} (default) an error is thrown when
attempting to overwrite an existing registration.}
}
\value{
The \code{name} string, invisibly.
}
\description{
Adds a new datatype to the bidser runtime registry so that
\code{\link{bids_project}} will scan and parse its folder.  The five
built-in datatypes (\code{"func"}, \code{"anat"}, \code{"fmap"},
\code{"funcprep"}, \code{"anatprep"}) can only be overwritten by passing
\code{overwrite = TRUE}.
}
\examples{
\dontrun{
my_spec   <- func_spec()   # placeholder
my_parser <- func_parser() # placeholder
register_datatype("dwi", spec = my_spec, parser_fn = my_parser,
                  folder = "dwi", scope = "raw")
list_datatypes()
}
}
