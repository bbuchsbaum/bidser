% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confounds.R
\name{check_confounds}
\alias{check_confounds}
\alias{clean_confounds}
\title{Check and clean confound tables}
\usage{
check_confounds(x, checks = c("zero_variance", "rank"), group_vars = NULL)

clean_confounds(
  x,
  clean = c("zero_variance", "rank"),
  group_vars = NULL,
  inform = TRUE
)
}
\arguments{
\item{x}{A confound table, typically a \code{bids_confounds} object.}

\item{checks}{Character vector of checks to run. Supported values are
\code{"zero_variance"} and \code{"rank"}.}

\item{group_vars}{Optional character vector of columns defining run-level
groups for flat tables.}

\item{clean}{Character vector of cleaning operations to apply. Supported
values are \code{"none"}, \code{"zero_variance"}, and \code{"rank"}.}

\item{inform}{Logical. If \code{TRUE}, report dropped columns with run labels.}
}
\value{
\code{check_confounds()} returns a tibble of diagnostics. \code{clean_confounds()}
returns \code{x} with flagged columns removed and a \code{confound_diagnostics}
attribute containing the same diagnostic rows.
}
\description{
\code{check_confounds()} reports nuisance columns that are unsuitable for model
matrices, such as zero-variance columns within a run. \code{clean_confounds()}
drops the flagged columns and stores the diagnostics on the returned object.
}
\details{
These helpers understand both nested \code{bids_confounds} objects returned by
\code{read_confounds(..., nest = TRUE)} and flat confound tables. For flat tables,
checks are run within the identifier columns present in the data
(\code{participant_id}, \code{task}, \code{session}, and \code{run} by default).
}
\examples{
df <- tibble::tibble(
  participant_id = "01",
  task = "rest",
  run = "01",
  cosine00 = c(1, 0, -1),
  cosine01 = c(0, 0, 0)
)
check_confounds(df)
clean_confounds(df)
}
