% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_extensions.R
\name{derivative_pipelines}
\alias{derivative_pipelines}
\title{List derivative pipelines in a BIDS project}
\usage{
derivative_pipelines(x)
}
\arguments{
\item{x}{A \code{bids_project} object.}
}
\value{
A tibble with one row per derivative pipeline and columns \code{pipeline},
\code{root}, \code{description}, and \code{source}.
}
\description{
Returns a tibble describing the derivative pipelines currently attached to a
\code{bids_project}. This is the preferred inspection entry point for new code;
legacy \code{fmriprep} / \code{prep_dir} fields remain available for compatibility.
}
\examples{
\donttest{
tryCatch({
  ds_path <- get_example_bids_dataset("ds000001-fmriprep")
  proj <- bids_project(ds_path, fmriprep = TRUE)
  derivative_pipelines(proj)
  unlink(ds_path, recursive = TRUE)
}, error = function(e) message("Example requires internet: ", e$message))
}
}
