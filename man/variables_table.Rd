% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_extensions.R
\name{variables_table}
\alias{variables_table}
\title{Create a tidy run-level variables table}
\usage{
variables_table(
  x,
  subid = ".*",
  task = ".*",
  run = ".*",
  session = ".*",
  include = c("events", "confounds"),
  scope = c("all", "raw", "derivatives"),
  pipeline = NULL
)
}
\arguments{
\item{x}{A \code{bids_project} object.}

\item{subid}{Regex pattern to match subject IDs.}

\item{task}{Regex pattern to match tasks.}

\item{run}{Regex pattern to match runs.}

\item{session}{Regex pattern to match sessions.}

\item{include}{Which variable sources to include. Supported values are
\code{"events"} and \code{"confounds"}.}

\item{scope}{Query scope used for scan inventory. One of \code{"all"}, \code{"raw"}, or
\code{"derivatives"}.}

\item{pipeline}{Optional derivative pipeline name to restrict derivative scan
inventory.}
}
\value{
A tibble with one row per run and list-columns \code{scans}, \code{events}, and
\code{confounds} when available.
}
\description{
Aggregates event tables, confound tables, and scan inventory into a single
run-level tibble with normalized identifiers and nested list-columns. This is
a lightweight variables layer for downstream R workflows rather than a full
model-spec engine.
}
