% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sidecar.R
\name{n_volumes}
\alias{n_volumes}
\alias{n_volumes.character}
\alias{n_volumes.bids_project}
\title{Get the number of volumes in functional scans}
\usage{
n_volumes(x, ...)

\method{n_volumes}{character}(x, ...)

\method{n_volumes}{bids_project}(
  x,
  subid = ".*",
  task = ".*",
  run = ".*",
  session = ".*",
  scope = c("raw", "derivatives"),
  as_tibble = FALSE,
  ...
)
}
\arguments{
\item{x}{A character vector of NIfTI paths, or a \code{bids_project} object.}

\item{...}{Additional arguments passed to methods.}

\item{subid}{Regex pattern to match subject IDs.}

\item{task}{Regex pattern to match task names.}

\item{run}{Regex pattern to match run IDs.}

\item{session}{Regex pattern to match session IDs.}

\item{scope}{Scan source. \code{"raw"} uses \code{\link[=func_scans]{func_scans()}}, while
\code{"derivatives"} uses \code{\link[=preproc_scans]{preproc_scans()}}.}

\item{as_tibble}{If \code{TRUE}, return \code{.path}, parsed entities, and \code{nvols}.}
}
\value{
For character input, a named integer vector with one value per path.
For \code{bids_project} input, a named integer vector by default, or a tibble
when \code{as_tibble = TRUE}.
}
\description{
Reads the NIfTI header for one or more BOLD files and returns the 4th data
dimension, i.e. the number of time points/volumes in each scan.
}
\examples{
\dontrun{
n_volumes("sub-01_task-rest_bold.nii.gz")
n_volumes(proj, subid = "01", task = "rest")
}
}
