% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/all_generic.R, R/query.R
\name{get_metadata}
\alias{get_metadata}
\alias{get_metadata.bids_project}
\title{Resolve metadata for a BIDS file}
\usage{
get_metadata(
  x,
  file,
  inherit = TRUE,
  scope = c("auto", "raw", "derivatives", "all"),
  ...
)

\method{get_metadata}{bids_project}(
  x,
  file,
  inherit = TRUE,
  scope = c("auto", "raw", "derivatives", "all"),
  ...
)
}
\arguments{
\item{x}{A \code{bids_project} object.}

\item{file}{Target file path (relative to project root or absolute path).}

\item{inherit}{If \code{TRUE}, merge inherited metadata from parent sidecars.}

\item{scope}{Scope used when applying inheritance:
\itemize{
\item \code{"auto"}: infer from file location
\item \code{"raw"}: raw data inheritance
\item \code{"derivatives"}: derivatives inheritance
\item \code{"all"}: allow full project ancestry
}}

\item{...}{Additional arguments for methods.}
}
\value{
A named list of metadata fields.
}
\description{
Retrieves JSON metadata for a target BIDS file. When \code{inherit = TRUE}, this
method applies BIDS-style inheritance by merging matching sidecars from less
specific to more specific locations.
}
\details{
If multiple sidecars are equally specific at the same directory depth, they
are merged in deterministic path order after less specific ancestors, so the
final values are reproducible.
}
\examples{
\donttest{
tryCatch({
  ds001_path <- get_example_bids_dataset("ds001")
  proj <- bids_project(ds001_path)
  f <- func_scans(proj, subid = "01")[1]
  if (!is.null(f)) get_metadata(proj, f)
  unlink(ds001_path, recursive = TRUE)
}, error = function(e) message("Example requires internet: ", e$message))
}
}
