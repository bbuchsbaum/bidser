% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{bids_entities}
\alias{bids_entities}
\title{Parse BIDS entities from paths into a tibble}
\usage{
bids_entities(paths, include_path = TRUE, coerce = TRUE)
}
\arguments{
\item{paths}{Character vector of paths or filenames.}

\item{include_path}{Logical. If \code{TRUE} (default), include a \code{.path} column
containing the original input.}

\item{coerce}{Logical. If \code{TRUE} (default), coerce numeric BIDS entities such
as \code{run} and \code{echo} to integer when possible.}
}
\value{
A tibble with one row per input path. Missing entities are \code{NA}.
}
\description{
\code{bids_entities()} is the vectorized counterpart to \code{\link[=encode]{encode()}}. It accepts a
character vector of full paths or bare filenames and returns one row per
input path with the union of parsed BIDS entity columns.
}
\examples{
bids_entities(c(
  "sub-01_task-rest_run-01_bold.nii.gz",
  "sub-01_task-rest_run-02_bold.nii.gz"
))
}
