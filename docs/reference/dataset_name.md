# Get the name of a BIDS dataset

Get the name of a BIDS dataset

## Usage

``` r
dataset_name(x, ...)

# S3 method for class 'bids_dataset_description'
dataset_name(x, ...)
```

## Arguments

- x:

  A `bids_dataset_description` object.

- ...:

  Additional arguments passed to methods.

## Value

A character scalar (or `NA_character_` if absent).
