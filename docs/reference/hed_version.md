# Get the HEDVersion field of a BIDS dataset description

Get the HEDVersion field of a BIDS dataset description

## Usage

``` r
hed_version(x, ...)

# S3 method for class 'bids_dataset_description'
hed_version(x, ...)
```

## Arguments

- x:

  A `bids_dataset_description` object.

- ...:

  Additional arguments passed to methods.

## Value

A character vector of HED version strings.
