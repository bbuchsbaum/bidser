# Create a regex-based parser from a specification

Create a regex-based parser from a specification

## Usage

``` r
create_regex_parser(spec)
```

## Arguments

- spec:

  A specification object

## Value

A parser function
