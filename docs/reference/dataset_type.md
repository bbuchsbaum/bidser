# Get the type of a BIDS dataset

Get the type of a BIDS dataset

## Usage

``` r
dataset_type(x, ...)

# S3 method for class 'bids_dataset_description'
dataset_type(x, ...)
```

## Arguments

- x:

  A `bids_dataset_description` object.

- ...:

  Additional arguments passed to methods.

## Value

A character scalar, typically `"raw"` or `"derivative"`.
