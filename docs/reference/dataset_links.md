# Get the DatasetLinks field of a BIDS dataset description

Get the DatasetLinks field of a BIDS dataset description

## Usage

``` r
dataset_links(x, ...)

# S3 method for class 'bids_dataset_description'
dataset_links(x, ...)
```

## Arguments

- x:

  A `bids_dataset_description` object.

- ...:

  Additional arguments passed to methods.

## Value

A named list mapping dataset names to paths or URIs.
